---
kind: deployment
name: sava

gateways:
  9050: sava/port

clusters:
  sava:
    services:
      breed:
        name: runner
        deployable: magneticio/sava:runner_1.0
        ports:
          port: 8081/http
