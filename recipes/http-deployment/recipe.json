{
  "name": "HTTP Deployment",
  "description": "Deploying single HTTP service.",
  "steps": [
    {
      "description": "Deploy service.",
      "run": {
        "method": "PUT",
        "resource": "deploy.yml",
        "await": [
          "blueprints:sava", "archive:update"
        ]
      },
      "cleanup": {
        "method": "DELETE",
        "resource": "undeploy.yml",
        "await": [
          "blueprints:sava", "archive:delete"
        ]
      }
    },
    {
      "description": "Send request to service and validate response.",
      "run": {
        "method": "PUT",
        "resource": "deploy.yml",
        "await": [
          "blueprints:sava", "archive:update"
        ]
      },
      "cleanup": {
        "method": "DELETE",
        "resource": "undeploy.yml",
        "await": [
          "blueprints:sava", "archive:delete"
        ]
      }
    }
  ]
}
