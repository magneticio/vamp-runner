---
kind: deployment
name: runner1
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
---
kind: deployment
name: runner2
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
---
kind: deployment
name: runner3
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
