---
kind: deployments
name: runner1
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
---
kind: deployments
name: runner2
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
---
kind: deployments
name: runner3
clusters:
  frontend:
    services:
      breed: frontend
  backend:
    services:
      breed: backend
